package clm.demo.mappers;

import org.mapstruct.Mapper;

/**
 * MapStruct mapper for PlaceholderDTO conversions.
 * No longer needed as PlaceholderDTO is used directly throughout the application.
 * Kept for future extension if mapping logic is required.
 */
@Mapper(componentModel = "spring")
public interface PlaceholderMapper {
    // Placeholder for future mapping implementations if needed
}

