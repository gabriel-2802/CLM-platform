package clm.user.demo;

import org.junit.jupiter.api.Test;

class DemoApplicationTests {

    @Test
    void contextLoads() {
        // Full-context integration test requires a running PostgreSQL at localhost:5434
        // Run the service with docker-compose and verify with the slice tests instead.
    }
}
